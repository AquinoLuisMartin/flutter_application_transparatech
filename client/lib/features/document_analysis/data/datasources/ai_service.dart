/// AI service API for OCR / ML document analysis
/// Implement calls to remote AI/OCR providers here.
class AiService {
  Future<Map<String, dynamic>> analyzeFile(String filePath) async {
    // TODO: call OCR/ML API and return structured result
    return {'text': 'extracted text', 'entities': []};
  }
}
