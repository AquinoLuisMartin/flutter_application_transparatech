import '../entities/document.dart';

class UploadDocument {
  // final DocumentRepository repository;
  // UploadDocument(this.repository);

  Future<String> call(Document document) async {
    // TODO: call repository.upload(document)
    return 'uploaded-id';
  }
}
