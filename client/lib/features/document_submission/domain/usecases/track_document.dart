class TrackDocument {
  // final DocumentRepository repository;
  // TrackDocument(this.repository);

  Future<Map<String, dynamic>?> call(String id) async {
    // TODO: call repository.track(id)
    return null;
  }
}
